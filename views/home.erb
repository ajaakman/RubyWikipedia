	<div class="content">
	
	<p>Welcome to WAD wiki. A free online encyclopedia of knowledge that can be viewed by anyone!</p>
	
	<p>Feel free to search for anything you desire! And if it does not exist then create it!</p>
	
	<p>Use the menu to the left to navigate the site!</p>
	
	
		<% a = Array.new %>
		<% total_char = 0 %>
		<% total_word = 0 %>
		
			<% 	Article.order(updated_at: :desc).each do |article| %>
				<% if article.approved == true && !a.include?(article.heading)  %>
				<div class="post">
				<h2>
				<%= article.heading %><br></h2>
				<p>		
				<%= article.content %><br><br>
				<% if $credentials[0] != '' %>
					<a href ='/editarticle/<%= article.id %>'>Edit this article</a>
				<% end %>
				</p>
				<p1>
				Author: <%= article.author %>	Approved On: <%= article.updated_at %><br>
				Character Count: <%= article.content.size %>	Word Count: <%= article.content.split.size %><br>
				<% total_char += article.content.size %>
				<% total_word += article.content.split.size %>
				<% end %>
				</p1>
				</div>
				<br>
			<% a.push article.heading %>
			
			<% end %>
		<div class="post">	
		<p1>
		Combined Character Count: <%= total_char %>		Combined Word Count: <%= total_word %><br>
		</p1>
		</div>
		
			
		
			<!--<div class="my_image">
			
				<img class="my_image" src="/testimage.png" alt="testimage"/>
			
			</div>-->
	
	</div>
	

	